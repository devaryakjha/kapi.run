{
  "name": "@kapi/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev --port 3000",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "test": "bun test",
    "test:routing": "bun run build && bun scripts/check-routes.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@knadh/oat": "0.7.1"
  },
  "devDependencies": {
    "@kapi/spec": "workspace:*",
    "typescript": "^6.0.3",
    "vite": "8.0.16"
  }
}
